import React from 'react';

import classes from './Toolbar.css';

const toolbar = (props) =>(
    <header>
        <div>MENU</div>
        <di>Logo</di>
        <nav>.....</nav>
    </header>
);

export default toolbar;